/** Reserved for future platform chrome slots (toolbar account panel is the primary UI). */
export function bootstrapWebChrome(): void {
  // no-op — account details live in ComposerBar AccountPanel
}
