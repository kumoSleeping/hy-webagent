# Copy to ~/.config/hy-webagent/deploy.env (chmod 600) — do NOT commit real secrets.
export HY_WEBAGENT_SSH_HOST=ssh-ykhm.kumo.ltd
export HY_WEBAGENT_SSH_USER=root
export HY_WEBAGENT_SSH_PASSWORD='your-root-password'
# Optional overrides:
# export HY_WEBAGENT_APP_ROOT=/opt/hy-webagent
# export HY_WEBAGENT_SERVICE=hy-webagent
# export HY_WEBAGENT_PORT=3002
